html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title WebSocet & webrtc
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 ZOOM CLONE
        main
            h3 Welcom to Zoom Clone
            div 
                form#nickName
                    input(text="text" placeholder="Write a nickname" required)
                    button(type="submit") Choose
            div 
                ul
                form#msgSend
                    input(type="text" placeholder="Write a message" required)
                    button(type="submit") send Msg
            
        script(src="public/js/frontend.js") 